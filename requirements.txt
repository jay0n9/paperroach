# Core runtime dependencies for the local Obsidian knowledge pipeline.
# Python >= 3.11 (uses stdlib tomllib).
ollama>=0.4.0
lancedb>=0.13.0
pyarrow>=15.0.0
pymupdf4llm>=0.0.17
pyyaml>=6.0

# OCR for scanned PDFs with no text layer (CPU, offline). Recommended:
rapidocr-onnxruntime>=1.3.0

# Optional, better PDF parsing (uncomment to use --ingester docling):
# docling>=2.0.0
